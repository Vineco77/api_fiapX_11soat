import { container } from 'tsyringe';

// Aqui será configurado o container de injeção de dependências
// Exemplo de registro futuro:
// container.registerSingleton<IVideoRepository>('VideoRepository', PrismaVideoRepository);

export { container };
